# eng-versioning(7): the single source of truth for this repo's version. The key
# is PAPI_VERSION (uppercase, `-`→`_`) — matching what eng-versioning derives
# from go.mod once the validator's module lands, or linters.eng-versioning.key.
export PAPI_VERSION=0.1.0
